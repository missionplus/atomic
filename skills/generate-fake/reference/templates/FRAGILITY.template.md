# Fragility Report — `<service-name>` fake

This is a list of the integration surfaces in `<service-name>` that are
**risky** from a testability point of view. They make this fake harder to
maintain, more likely to drift, and more likely to cause "passes against the
fake, fails against the real thing" incidents.

Each finding includes a concrete suggestion the application team can act on.

---

## Summary

- High severity: `<n>`
- Medium severity: `<n>`
- Low severity: `<n>`

---

## High severity

### `H2` — `POST /orders`

**Surface:** `POST /orders` (`src/routes/orders.py:22`)
**Finding:** Request and response schemas are only defined in code (Pydantic),
not in the OpenAPI spec. The fake will drift silently if the model is changed
without updating the spec.
**Suggestion:** Generate the OpenAPI from the Pydantic models at build time,
or maintain a single hand-written spec that the code conforms to. Add a
CI check that fails if they diverge.

### `Q1` — topic `payments.received`

**Surface:** Inbound Kafka topic `payments.received` (`src/consumers/payments.py:9`)
**Finding:** No schema declared for the topic. Multiple event shapes appear in
recorded traffic.
**Suggestion:** Adopt AsyncAPI for this topic; split distinct event shapes
into distinct topics OR introduce a `type` discriminator with a closed enum.

---

## Medium severity

### `DB1` — table `users`

**Surface:** `users` table
**Finding:** No migration files; ORM models are the only schema source.
**Suggestion:** Introduce Alembic/Flyway/Prisma migrations so the fake's
SQLite stand-in has a verifiable schema source.

---

## Low severity

(none)

---

## Out of scope for this fake

Surfaces the fake does **not** cover, and why:

- `https://api.stripe.com` (outbound) — consumers must provide their own
  Stripe fake when testing flows that depend on it.
- `https://api.sendgrid.com` (outbound) — same.

---

_Generated by **Atomic** (`generate-fake` skill), a Claude Code plugin._
