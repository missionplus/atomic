# Lightweight Node fake. Target image size: < 200 MB.

FROM node:20-alpine AS runtime

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install --omit=dev

COPY . .

ENV FAKE_PORT=8000
EXPOSE 8000

CMD ["node", "app.js"]
